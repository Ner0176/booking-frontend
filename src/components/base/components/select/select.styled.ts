import { styled } from "styled-components";
import tw from "twin.macro";

export const StyledSelect = styled.select(
  tw`w-full p-2 !pr-4 rounded-xl border border-neutral-200 cursor-pointer !outline-none`
);
